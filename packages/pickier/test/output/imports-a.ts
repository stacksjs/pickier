import type { C, G as F } from 'lib'
import type { Y } from './local'
import 'side-effects'
import z, { B, E as D } from 'lib'
import * as NS from './ns'
import { X } from './local'

const v = B + (NS ? 1 : 0) + X
export { v }
