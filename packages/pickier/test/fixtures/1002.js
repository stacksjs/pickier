// JS complex fixture: functions, arrays, objects, quotes, whitespace
function sum(a, b){
  return a + b
}

const arr = ["x", "y", "z"]
const obj = {"a":1, "b":2}

if (arr.length > 0) {
  console.log("items:", arr.join(","))
}

